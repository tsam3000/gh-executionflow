name: Reusable Deploy
on: workflow_call # allows this workflow to be called from other workflows
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output Information
        run: echo "Deploying & uploading..."